{
  "name": "space-router",
  "description": "All the routing essentials.",
  "version": "1.3.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "src"
  ],
  "license": "ISC",
  "sideEffects": false,
  "repository": {
    "type": "git",
    "url": "git://github.com/KidkArolis/space-router.git"
  },
  "keywords": [
    "browser",
    "router",
    "small",
    "clientside",
    "universal",
    "history",
    "location",
    "url"
  ],
  "engines": {
    "node": ">=18"
  },
  "author": "Karolis Narkevicius",
  "scripts": {
    "test": "npm run typecheck && npm run build && oxlint -c oxlintrc.json && oxfmt --check '**/*.{ts,js,css,yml}' && c8 ava",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "oxfmt --write '**/*.{ts,js,css,yml}'",
    "coverage": "c8 --reporter=html ava",
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "watch": "tsc -p tsconfig.build.json --watch",
    "release": "np --no-release-draft",
    "release:beta": "np --tag=beta --no-release-draft",
    "release:docs": "hugo -s docs && gh-pages -d docs/public"
  },
  "devDependencies": {
    "ava": "^8.0.1",
    "c8": "^12.0.0",
    "gh-pages": "^6.3.0",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2"
  },
  "ava": {
    "extensions": [
      "ts"
    ]
  }
}
